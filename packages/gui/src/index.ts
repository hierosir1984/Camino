// @camino/gui — the browser app package. The React + Vite application is
// introduced by WP-102 (daemon-served build) and grows through WP-122/123/124.
export { formatStateLabel } from "./format.js";
