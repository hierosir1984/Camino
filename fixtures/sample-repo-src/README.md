# sample-repo

A deliberately tiny target repository, materialized into a real git repo by
`scripts/make-sample-repo.mjs`. It is the seeded fixture required by the
validatable-repo profile (WP-000) and grows into the onboarding/quarantine
fixture repo used by later work packages.
