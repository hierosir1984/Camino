# dependencies / build
node_modules/
dist/
coverage/
*.tsbuildinfo

# local gate record (the committed attestations live in docs/plan/)
.camino/gate-record.json

# load-test evidence records (disposable; scripts/load-test.mjs writes them)
.camino/load-test/

# transient fence-trip fixtures created by tests
**/__fence_trip__*

# WP-105 real-CLI smoke evidence (disposable; the durable record is the PR)
packages/daemon/dispatch-smoke/

# raw spike transcript streams (disposable; REPORT.md + summary.json are committed)
spikes/dispatch/transcripts/*.jsonl
spikes/plan-probe/transcripts/*.jsonl
spikes/untrusted-content/transcripts/*.jsonl
# raw agent evidence may contain whatever a worker read — never commit it (review r1 blocker 2)
spikes/untrusted-content/transcripts/raw-evidence.json

# OS noise
.DS_Store

# Playwright output (WP-122 GUI suite)
test-results/
playwright-report/
