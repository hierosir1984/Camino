# Camino — PRD Round-1 Review Dispositions

> 2026-07-16. Companion to [20-adversarial-review-prd-round1.md](20-adversarial-review-prd-round1.md). All 18 findings verified (CONFIRMED by cross-reading PRD v1.1 against design v5 and the founding brief; Grok capability receipt PLAUSIBLE, cited xAI docs, consistent) and accepted. Resolutions land in PRD v1.2.

## Blockers (10)

| # | Finding | Resolution in PRD v1.2 |
|---|---|---|
| 1 | Phase 1's merge/completion gate depends on P2 mechanisms (probes, semantic review, verification methods, completion semantics); gap-register table wrongly P2 | Phase chain repaired to match design v0 exactly: new **CAM-VAL-13 [P1]** mission-level integration check suite (planner-authored, worker-immutable) as the v0 stand-in for per-requirement probes; **CAM-VAL-06 split** — mission-level cross-family review [P1], issue-level risk-tiered review [P2]; **CAM-MERGE-05** re-scoped (P1 gate = mission checks green + mission review; full per-requirement suite as probes accumulate); **CAM-CANON-10** carries explicit P1 semantics (mission-checked + human approval); **CAM-CORE-09 → P1** |
| 2 | `camino/candidates/<uuid>` refs escape the CI restrictions — privileged-CI path reopened | **CAM-SEC-03** extended: Actions restrictions cover *all Camino-managed namespaces* (`mission/*`, issue branches, `camino/**`); onboarding verifies no workflow trigger can fire on those namespaces with secrets/write tokens; the quarantine attack suite gains a candidate-ref workflow case |
| 3 | Evidence schema lost per-item identity, advisory/gating classification, exclusions, waivers | Registry item 8 rewritten: `checks[]`/`reviews[]` carry per-item `{sha, base_sha, class: advisory\|gating}`; `exclusions[]` and `waivers[]` added; viewer renders the gating/advisory distinction |
| 4 | Quick tasks bypass the direct-to-main eligibility gates | **CAM-MERGE-01** now enforces all three design conditions (single-issue ∧ neutral-agreed ∧ non-sensitive); failing any routes through an integration branch |
| 5 | Transition table circularly deferred (registry → CAM-STATE-05 → "later") | **Appendix A written**: full mission/issue/attempt state tables with events, guards, and interrupt behavior; CAM-STATE-05 and registry item 4 point at it |
| 6 | Grok requirements mutually inconsistent (non-goals, acceptance, spike wording, quota registry) + "unverified" stale | Consistency sweep: §2 non-goals now GLM-only; CAM-EXEC-01 acceptance = enabled adapters pass the spike, xAI's official headless support recorded (technical path verified per xAI docs; contractual check remains at onboarding); Phase 0 exit = "all enabled adapters"; registry item 13 adds Grok windows |
| 7 | Mission→main PR audit lifecycle missing | New **CAM-MERGE-13 [P1]**: mission PR opened with the mission branch, carries requirement checklist + evidence links, is the review/audit surface, marked merged by the landing push, retained |
| 8 | No dependency/readiness contract (founding-brief drift) | New **CAM-PLAN-11 [P1]** (plans emit dependency edges; DAG validated acyclic) and **CAM-PLAN-12 [P1]** (ready = dependencies merged into the mission branch; v1 executes issues sequentially per mission; within-mission concurrency [F]); downstream reconciliation wired to CAM-PLAN-05 |
| 9 | "Risk-tiered" invoked with no implementable policy | New registry item 18: three tiers with deterministic assignment (sensitive-area path rules floor the tier: auth/authz, payments, data migration, secrets = high; user-observable behavior = medium; internal/docs/tests = low); planner proposes, rules floor it, user may raise but never lower below the floor; persisted per issue; drives review depth and the autonomy joint-distribution guard |
| 10 | User-observability classification lost the reviewer lock (paths-only) | **CAM-PLAN-06/08** + registry item 2: observability = path heuristics OR planner judgment, and reviewer concurrence is required to classify any heuristic-flagged item as *not* observable |

## Deferrables (8)

11 Orchestrator channel unbound to T2 controls → CAM-EXEC-10/11 gain provenance-tagged responses bound to the attempt's contract version and context snapshot; CAM-SEC-07 red-team scope now includes the orchestrator channel; the "provably routes" acceptance is narrowed to red-team fixture cases. 12 Temporal view overclaims objective history → CAM-CANON-11 reworded: reconstructs *Camino's recorded state* (projections), which is what the event log can honestly support. 13 API fallback load-bearing but [F] → CAM-ROUTE-08 made honest and implementable now: the fallback path is the *same official CLIs re-authenticated with funded API keys* (a config switch both CLIs natively support), documented as a runbook; the [F] API-key adapter covers *additional* providers. 14 Omitted v5 details → restored: reserved-name alias rejection + worker-attribution trailer (CAM-EXEC-04); knowledge scope/expiry/contradiction-curation (CAM-CANON-09); plan-time validation-resource declaration (new CAM-VAL-14 [P2]); single fenced validation-environment owner (CAM-STATE-04); canon enums incl. `resolved-accepted`, non-inheritance and reverse-transition rules (CAM-CANON-03). 15 Acceptance clauses test less than their requirements → strengthened on the cited requirements (CORE-01/02/04/07, PLAN-01/05, EXEC-10). 16 Safety-critical requirements lack acceptance paths → Accept: clauses added to CAM-VAL-03 (egress + scrubbing tests), CAM-SEC-03 (hostile-workflow fixture), CAM-CANON-06 (external-commit fixture), CAM-STATE-02 (seeded duplicate-intent fixtures); Phase 0 gains the egress/scrubbing experiment from design §11. 17 Chaos test and phase exits unmeasurable → CAM-STATE-06 uses deterministic seeded kill points covering every side-effect class; Phase 2 "no gross failure" defined as zero seeded-defect approvals in the calibration replay; Phase 3 injection exit = findings dispositioned with recorded accept/reject. 18 Broken §-references and incomplete attack-suite enumeration → all internal references repaired; Phase 0 quarantine suite enumerates symlink-target, reserved-name, CI-definition, scope, worker-merge-commit, and candidate-ref workflow cases.

## Bottom line

Sol's verdict, verbatim: **"safe to build from: no — blockers: repair the P1 probe/review/completion phase chain; secure candidate refs; restore per-item evidence identity; enforce quick-task gates; specify the full transition table; reconcile Grok scope, spike, policy, and quota requirements; require the mission PR audit lifecycle; define dependency/readiness and risk policies; and restore reviewer-based observability classification; deferrables: bind and red-team the orchestrator channel, narrow temporal history to recorded state, make API fallback honest, restore omitted v5 details, complete acceptance criteria and experiments, replace random/qualitative phase gates with executable thresholds, and repair references and attack suites."**

All accepted → PRD v1.2. Round 2 verifies closure; same stopping rule (new blockers → human triage).
