# Pin workspace behavior explicitly: the development machine's global ~/.npmrc
# sets workspaces=true (which would exclude the root and skip devDependencies
# on install); vanilla CI has no such flag. Pinning both keys makes install and
# script scoping identical everywhere: root + all workspaces.
workspaces=true
include-workspace-root=true
